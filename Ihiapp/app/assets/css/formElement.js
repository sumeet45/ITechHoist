import { StyleSheet } from "react-native";

const formElementStyles = StyleSheet.create({
    input: { height: 40, marginBottom: 15, borderWidth: 1, padding: 10, color: "#000" },
    submitContainer: { backgroundColor: "red", padding: 6, marginBottom: 20 },
    submitButton: { paddingVertical: 10, color: "#fff", fontWeight: "bold", fontSize: 15, alignSelf: "center", textTransform: "uppercase" },
});

export default formElementStyles;